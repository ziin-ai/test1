#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../astro/astro.js" "$@"
else
  exec node  "$basedir/../../../../astro/astro.js" "$@"
fi
# cmd-shim-target=/Users/choonhoson/ziin-ai/dashboard/node_modules/.pnpm/@astrojs+tailwind@6.0.2_astro@4.16.19_rollup@4.60.3_typescript@6.0.3__tailwindcss@4.2.4/node_modules/astro/astro.js
